# Env names only. Copy to .env locally. Never commit .env.

# Comma-separated principals who may also search document chunks.
GATED_RECALL_OPERATORS=

# Cap on injected recall characters (primary lane only).
GATED_RECALL_MAX_CHARS=2000
